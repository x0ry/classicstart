# Build output (run build.bat to regenerate)
*.exe
*.obj
*.pdb
*.ilk

# Runtime config (ClassicStart writes this with defaults on first launch)
classicstart.ini

# Local editor/tooling state
.claude/
.vs/
